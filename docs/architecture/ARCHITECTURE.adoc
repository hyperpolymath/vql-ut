// SPDX-License-Identifier: PMPL-1.0-or-later
// Copyright (c) 2026 Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= VCL-total Architecture
Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
:date: 2026-03-16
:toc: macro
:toclevels: 3
:icons: font
:description: Comprehensive architecture document for VCL-total (Verified Query Language — Unified Types)

toc::[]

== System Overview

VCL-total (Verified Query Language — Unified Types) is a dependently-typed query
language compiler that produces machine-checkable proofs of query safety. It
extends VCL-dt (VeriSimDB's existing dependent type system, ~35% wired) to
cover the full spectrum of safety properties through 10 formally verified
levels.

Every query entering VCL-total passes through a six-stage pipeline. Each stage
is responsible for enforcing one or more of the 10 safety levels. The pipeline
output is not merely a compiled query — it is a *proof certificate* attesting
that every safety property holds.

=== Pipeline: Parse → Bind → Check → Analyse → Compile → Execute

[source]
----
                          VCL-total PIPELINE
                          ═══════════════

  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
  │   1. PARSE   │───▶│   2. BIND    │───▶│   3. CHECK   │
  │              │    │              │    │              │
  │ ReScript     │    │ Idris2       │    │ Idris2       │
  │ Tokenise &   │    │ Resolve      │    │ Type-check   │
  │ parse VCL-total │    │ names &      │    │ all 10       │
  │ surface      │    │ schema refs  │    │ safety       │
  │ syntax       │    │              │    │ levels       │
  └──────────────┘    └──────────────┘    └──────────────┘
         │                   │                   │
         │ AST               │ Bound AST         │ Typed AST +
         │                   │                   │ Obligations
         ▼                   ▼                   ▼
  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
  │  4. ANALYSE  │───▶│  5. COMPILE  │───▶│  6. EXECUTE  │
  │              │    │              │    │              │
  │ Idris2       │    │ Zig          │    │ Elixir       │
  │ Discharge    │    │ Generate     │    │ Dispatch to  │
  │ proof        │    │ optimised    │    │ VeriSimDB    │
  │ obligations  │    │ query plan   │    │ engine       │
  │              │    │ via C ABI    │    │              │
  └──────────────┘    └──────────────┘    └──────────────┘
         │                   │                   │
         │ Proof             │ Native            │ Result +
         │ Certificate       │ Query Plan        │ Certificate
         ▼                   ▼                   ▼
  ┌─────────────────────────────────────────────────────┐
  │              PROOF CERTIFICATE OUTPUT                │
  │                                                     │
  │  Machine-checkable evidence that the query passed   │
  │  all 10 safety levels. Independently verifiable.    │
  └─────────────────────────────────────────────────────┘
----

== The 11 Safety Levels

VCL-total enforces 11 distinct safety levels (L0–L10), each targeting a specific
class of bug. The levels are cumulative — a query at Level N has passed all
checks from Levels 0 through N.

[cols="1,2,3,2,2"]
|===
|Level |Name |What It Prevents |Idris2 Module |Pipeline Stage

|L1
|Construction Safety
|SQL/VCL injection attacks
|`Injection.idr`
|Parse + Bind

|L2
|Schema Pinning
|Schema drift (query vs database mismatch)
|`SchemaEvol.idr`
|Bind

|L3
|Resource Linearity
|Connection leaks, double-use bugs
|`Linear.idr`
|Check

|L4
|Session Protocols
|Use-after-commit, auth bypass, double-close
|`Session.idr`
|Check

|L5
|Effect Tracking
|Undeclared side effects (writes in read-only context)
|`Effects.idr`
|Check

|L6
|Scope Isolation
|Cross-transaction data leaks
|`Modal.idr`
|Check

|L7
|Information Flow
|Security classification violations (secret → public)
|`InfoFlow.idr`
|Check + Analyse

|L8
|Quantitative Bounds
|Resource overuse beyond declared limits
|`Quantitative.idr`
|Check

|L9
|Proof Attachment
|Unverified claims about query results
|`ProofCarrying.idr`
|Analyse

|L10
|Cross-Cutting Invariants
|Violations of compositional properties
|`Proofs.idr`
|Analyse

|L11
|Epistemic Safety
|Inconsistent knowledge states across federated agents; unverified claims treated as knowledge; circular epistemic dependencies
|`Epistemic.idr`
|Check
|===

NOTE: Level 11 (Epistemic Safety) uses S5 modal logic with Kripke frame
semantics embedded in the Idris2 type system. The EPISTEMIC clause specifies
agents, their knowledge/belief requirements, and common knowledge properties.
The type checker becomes the epistemic model checker — a term of type
`Knows a P` is constructive evidence that agent `a` knows `P`.

=== Level Detail

==== L1: Construction Safety (Injection.idr)

Queries are unforgeable by construction. The `SafeQuery` type is an opaque AST
indexed by a parameter schema. There is no function `String -> SafeQuery` — user
input can only appear in typed parameter slots, never in query structure
positions. Injection is impossible because the path from untrusted input to
query structure simply does not exist in the type system.

==== L2: Schema Pinning (SchemaEvol.idr)

Every query is indexed by the schema version it was validated against. A
`SchemaQuery v` can only execute against a database at version `v`. Schema
migrations produce `Migration from to` proofs, and queries must be explicitly
re-validated via `migrateQuery` with evidence that all referenced columns
survive the migration.

==== L3: Resource Linearity (Linear.idr)

Database connections carry a usage count at the type level. A `LinConn (S n)`
must be consumed exactly once per scope — the Idris2 compiler enforces this
via QTT quantity `1`. Dropped connections are compile-time errors. Double-use
is impossible.

==== L4: Session Protocols (Session.idr)

Sessions are state machines indexed by their protocol state. The type
`Session : SessionState -> Type` ensures transitions follow the protocol:
`Fresh → Authenticated → InTransaction → Committed → Closed`. Querying after
commit, committing without a transaction, and closing twice are all type errors.

==== L5: Effect Tracking (Effects.idr)

Each query declares its effects (`Read`, `Write`, `Cite`, `Audit`, `Transform`,
`Federate`). The `Subsumes` proof verifies that actual operations are a subset
of declared effects. A query declared `EFFECTS { Read }` that attempts a write
is rejected at compile time.

==== L6: Scope Isolation (Modal.idr)

Data accessed in one transaction scope is wrapped in `Box w a`, where `w` is a
world tag representing the scope. Extracting data requires proof of being in the
same scope (`InScope w`). Cross-scope data flow requires explicit `marshal`
calls, making all boundary crossings visible in the type signature.

==== L7: Information Flow (InfoFlow.idr)

Data carries security classifications (`Public`, `Internal`, `Confidential`,
`Secret`, `TopSecret`) forming a lattice. The `FlowsTo` relation ensures data
can only move upward in classification. Reading a `Secret` value requires
`HasClearance Secret` evidence and a `FlowsTo Secret readerLevel` proof.
Downward flows are impossible — the constructor does not exist.

==== L8: Quantitative Bounds (Quantitative.idr)

Generalises linearity from "exactly once" to "at most N". A
`BoundedResource n a` can be consumed `n` times before depletion. Parallel
query branches can split a resource budget via `merge`/`split`. This enforces
API rate limits, connection pool bounds, and quota compliance at compile time.

==== L9: Proof Attachment (ProofCarrying.idr)

Query results carry formal theorems as dependent pairs:
`ProvedResult a thm` bundles data with a proof of integrity, freshness,
provenance, or consistency. The proof can be independently verified via
`verify`, which returns `Either VerifyError (result : a ** Holds thm result)`.

==== L10: Cross-Cutting Invariants (Proofs.idr)

Compositional properties that span multiple levels: subset transitivity,
subsumption monotonicity, flow lattice transitivity. These proofs ensure that
the safety levels compose correctly — e.g., adding effects never breaks
existing subsumption proofs, and chained migrations preserve column survival.

== Technology Stack

[cols="1,2,3"]
|===
|Technology |Role |Rationale

|**Idris2**
|ABI definitions, type kernel, proof obligations
|Native QTT (quantity `0`/`1`/`ω`) makes linear types and resource tracking
first-class. Dependent types enable schema-indexed queries. `%default total`
with zero `believe_me` guarantees all proofs are real.

|**Zig**
|FFI bridge, native codegen
|C-ABI compatible without overhead. Cross-compilation built-in. Zero runtime
dependencies. Exports the type checker's validation functions to non-Idris2
consumers via the hyperpolymath ABI/FFI standard.

|**ReScript**
|Surface syntax parser
|Consistent with existing VCL parser patterns in VeriSimDB. Compiles to
JavaScript for browser-based tooling. Extends the VCL v3.0 grammar with 6
new optional clauses.

|**V-lang**
|HTTP API gateway
|Exposes VCL-total as a REST API for external consumers. Fast compilation,
minimal runtime. Follows the hyperpolymath V-lang API triple pattern.

|**Elixir**
|VeriSimDB integration, query orchestration
|BEAM runtime for distributed query dispatch. Phoenix for real-time feedback.
Zig NIFs for performance-critical paths. Native supervision trees for fault
tolerance.
|===

=== Technology Interaction

[source]
----
  ┌──────────────────────────────────────────────────────────┐
  │                     CLIENT (Browser/CLI)                  │
  └──────────────────────┬───────────────────────────────────┘
                         │ HTTP/WebSocket
                         ▼
  ┌──────────────────────────────────────────────────────────┐
  │               V-LANG API GATEWAY (v-api/)                │
  │  Routes: POST /check, POST /compile, GET /levels/:id     │
  └──────────────────────┬───────────────────────────────────┘
                         │ FFI (C ABI)
                         ▼
  ┌──────────────────────────────────────────────────────────┐
  │               ZIG FFI BRIDGE (ffi/zig/)                   │
  │  Exports: vqlut_parse, vqlut_check, vqlut_compile         │
  │  Generates: C headers (generated/abi/*.h)                 │
  └──────┬──────────────────────────────┬────────────────────┘
         │ Parse                         │ Check + Compile
         ▼                               ▼
  ┌────────────────┐          ┌─────────────────────────────┐
  │ RESCRIPT PARSER │          │ IDRIS2 TYPE KERNEL          │
  │ (src/parser/)   │          │ (src/abi/)                  │
  │                 │          │                             │
  │ Tokenise        │          │ 12 modules:                 │
  │ Parse           │──AST──▶  │ Core + 10 safety levels +   │
  │ Surface check   │          │ Checker (unified validator) │
  └─────────────────┘          └──────────────┬──────────────┘
                                              │ Proof Certificate
                                              ▼
                               ┌─────────────────────────────┐
                               │ ELIXIR ORCHESTRATION         │
                               │ (elixir/)                    │
                               │                              │
                               │ Dispatch to VeriSimDB engine │
                               │ Real-time feedback via       │
                               │ Phoenix Channels             │
                               │ Zig NIFs for hot paths       │
                               └──────────────────────────────┘
----

== The TypeLL Connection

VCL-total's 10 safety levels map 1:1 to TypeLL's 10 type theory levels. This is
not a coincidence — VCL-total is the *database specialisation* of TypeLL's
general-purpose type system.

[cols="1,2,2,2"]
|===
|Level |TypeLL Concept |VCL-total Instantiation |Module

|1
|Construction Types
|Unforgeable query ASTs
|`Injection.idr`

|2
|Indexed Types
|Schema-versioned queries
|`SchemaEvol.idr`

|3
|Linear Types
|Resource-counted connections
|`Linear.idr`

|4
|Session Types
|Database protocol state machines
|`Session.idr`

|5
|Effect Types
|Declared vs actual effect sets
|`Effects.idr`

|6
|Modal Types
|Transaction scope isolation
|`Modal.idr`

|7
|Information Flow Types
|Security classification lattice
|`InfoFlow.idr`

|8
|Quantitative Types
|Bounded resource accounting
|`Quantitative.idr`

|9
|Proof-Relevant Types
|Theorem-carrying query results
|`ProofCarrying.idr`

|10
|Metatheoretic Types
|Cross-cutting compositional invariants
|`Proofs.idr`
|===

TypeLL provides the *theory*; VCL-total provides the *practice*. Every TypeLL level
has a concrete, tested implementation in VCL-total's Idris2 kernel. This makes
VCL-total both a useful tool and a validation of TypeLL's design.

== Data Flow

=== Parse Stage

[source]
----
  VCL-total Source Text
  ─────────────────
  "SELECT GRAPH, DOCUMENT
   FROM HEXAD 550e8400-...
   WHERE FULLTEXT CONTAINS $query
   CONSUME AFTER 1 USE
   EFFECTS { Read, Cite }
   PROOF ATTACHED IntegrityTheorem"

          │
          │ ReScript Tokeniser
          ▼

  Token Stream
  ────────────
  [SELECT, GRAPH, COMMA, DOCUMENT, FROM, HEXAD, UUID(...),
   WHERE, FULLTEXT, CONTAINS, PARAM("query"),
   CONSUME, AFTER, INT(1), USE,
   EFFECTS, LBRACE, READ, COMMA, CITE, RBRACE,
   PROOF, ATTACHED, IDENT("IntegrityTheorem")]

          │
          │ ReScript Parser
          ▼

  Untyped AST (TQLAst.res)
  ─────────────────────────
  Select {
    modalities: [Graph, Document],
    source: Hexad("550e8400-..."),
    where: Fulltext(ParamRef("query")),
    extensions: [
      ConsumeAfter(1),
      Effects([Read, Cite]),
      ProofAttached("IntegrityTheorem")
    ]
  }
----

=== Bind Stage

[source]
----
  Untyped AST + Schema v3
  ─────────────────────────

          │
          │ Idris2 Schema Resolution
          │ (SchemaEvol.idr)
          ▼

  Bound AST
  ──────────
  SchemaQuery 3 {
    select: SafeSelect [Graph, Document] hexadRef,
    params: [MkTypedParam "query" PString],
    schema: schemaV3
  }

  + ColumnExists proofs for each referenced column
  + SchemaQuery indexed by version 3
----

=== Check Stage

[source]
----
  Bound AST
  ──────────

          │
          │ 10 Safety Level Checks (Idris2)
          ▼

  Typed AST + Proof Obligations
  ─────────────────────────────
  L1:  SafeQuery searchSchema          ✓ (construction safety)
  L2:  SchemaQuery 3                    ✓ (schema pinning)
  L3:  LinConn 1                        ✓ (single use)
  L4:  Session InTransaction            ✓ (protocol state)
  L5:  Subsumes [Read,Cite] [Read,Cite] ✓ (effects subset)
  L6:  Box txWorld result               ✓ (scope isolation)
  L7:  Labeled Public result            ✓ (classification)
  L8:  BoundedResource 1 conn          ✓ (quantitative)
  L9:  ProvedResult a IntegrityThm      ○ (obligation: prove integrity)
  L10: flowTransitive, subsetTransitive ✓ (compositional)

  Outstanding obligations: [IntegrityTheorem discharge]
----

=== Analyse Stage

[source]
----
  Typed AST + Obligations
  ────────────────────────

          │
          │ Proof Discharge (Idris2)
          ▼

  Proof Certificate
  ─────────────────
  Certificate {
    query_hash: "sha256:abc123...",
    safety_levels: [L1..L10],
    each_level: PASS,
    proofs_discharged: [IntegrityTheorem],
    verified_at: "2026-03-16T10:30:00Z",
    schema_version: 3,
    idris2_version: "0.8.0"
  }
----

=== Compile + Execute Stages

[source]
----
  Proof Certificate + Typed AST
  ──────────────────────────────

          │
          │ Zig Codegen (ffi/zig/)
          ▼

  Native Query Plan
  ─────────────────
  Optimised execution plan with:
  - Parameter binding slots
  - Modality access pattern
  - Resource budget annotations

          │
          │ Elixir Dispatch
          ▼

  VeriSimDB Engine
  ────────────────
  Execute query plan, return result + certificate
----

== Error Propagation

Each safety level produces typed errors that propagate through the pipeline:

[cols="1,2,3"]
|===
|Level |Error Type |Example

|L1
|`InjectionError`
|"Untrusted string in query structure position"

|L2
|`SchemaMismatch`
|"Column 'age' does not exist at schema version 2"

|L3
|`LinearityViolation`
|"Connection used twice in linear context"

|L4
|`ProtocolViolation`
|"Cannot query in state Committed"

|L5
|`EffectViolation`
|"Write effect not declared (declared: [Read])"

|L6
|`ScopeEscape`
|"Data from transaction scope 'tx1' leaked to 'tx2'"

|L7
|`ClassificationViolation`
|"Secret data cannot flow to Public context"

|L8
|`ResourceExhausted`
|"Resource limit 5 exceeded (attempted 6 uses)"

|L9
|`ProofFailure`
|"IntegrityTheorem could not be discharged"

|L10
|`CompositionError`
|"Subsumption monotonicity violated after effect addition"
|===

Errors are *structured*, not string messages. Each error carries the evidence
needed to understand and fix the problem — including the safety level, the
specific obligation that failed, and the source location.

== The Proof Certificate Concept

A proof certificate is a machine-checkable artefact produced by VCL-total's
pipeline. It attests that a query has passed all applicable safety levels.

Properties of a proof certificate:

1. **Independently verifiable**: Any party with the Idris2 type kernel can
   re-check the certificate without trusting VCL-total's implementation.

2. **Tamper-evident**: The certificate includes a SHA-256 hash of the query
   AST. Modifying the query invalidates the certificate.

3. **Level-granular**: The certificate reports each safety level individually.
   A query might pass L1–L8 but fail L9 (proof attachment). This granularity
   enables graduated deployment.

4. **Composable**: Certificates for sub-queries compose into certificates for
   compound queries, subject to L10 (cross-cutting invariant) checks.

5. **Versioned**: Certificates are pinned to schema version, Idris2 kernel
   version, and VCL-total grammar version. Version mismatches require
   re-certification.

=== Certificate Format

[source,json]
----
{
  "version": "1.0.0",
  "query_hash": "sha256:2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824",
  "schema_version": 3,
  "certified_at": "2026-03-16T10:30:00Z",
  "kernel_version": "vcl-total-0.1.0",
  "idris2_version": "0.8.0",
  "levels": {
    "L1_construction":  { "status": "pass", "module": "Injection.idr" },
    "L2_schema":        { "status": "pass", "module": "SchemaEvol.idr" },
    "L3_linearity":     { "status": "pass", "module": "Linear.idr" },
    "L4_session":       { "status": "pass", "module": "Session.idr" },
    "L5_effects":       { "status": "pass", "module": "Effects.idr" },
    "L6_modal":         { "status": "pass", "module": "Modal.idr" },
    "L7_infoflow":      { "status": "pass", "module": "InfoFlow.idr" },
    "L8_quantitative":  { "status": "pass", "module": "Quantitative.idr" },
    "L9_proof":         { "status": "pass", "module": "ProofCarrying.idr" },
    "L10_metatheory":   { "status": "pass", "module": "Proofs.idr" }
  },
  "obligations_discharged": ["IntegrityTheorem", "FreshnessTheorem"],
  "signature": "ed25519:..."
}
----

== Licence

PMPL-1.0-or-later
